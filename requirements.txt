# ============================================================
#   Ultroid — Python Dependencies
#   Install: pip install -r requirements.txt
#   Note: Heavy C/Rust packages (Pillow, lxml, etc.) are
#         handled automatically by installer.sh on Termux.
# ============================================================

# --- Core Runtime ---
telethon
telethon-patch
python-decouple
python-dotenv
pytz
gitpython

# --- Web & HTTP ---
aiohttp
requests
cloudscraper

# --- Database Backends (install only what you configure in .env) ---
redis
hiredis            # Optional speedup for Redis
pymongo[srv]       # For MongoDB Atlas
psycopg2-binary    # For PostgreSQL / Supabase
localdb.json       # Default local JSON DB (fallback, always installed)

# --- AI & Search ---
groq
duckduckgo_search

# --- Media & File Processing ---
Pillow
yt-dlp>=2026.3.17
youtube-search-python
aria2p             # Optional: Aria2 RPC for external download management

# --- Media Metadata ---
beautifulsoup4
lxml

# --- System & Monitoring ---
psutil
apscheduler
coloredlogs
tabulate

# --- Misc Integrations ---
telegraph
enhancer
catbox-uploader
google-api-python-client
oauth2client
speedtest-cli

# --- Addon-Specific Dependencies ---
# These are required by several plugins in the /addons folder.
pyfiglet
wikipedia
pybase64
instaloader
python-magic